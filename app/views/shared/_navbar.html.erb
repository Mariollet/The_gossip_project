<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= welcome_path %>"><strong>The Gossip Project</strong></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="<%= gossips_path %>"> Gossips </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= users_path %>"> Users </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= cities_path %>"> Villes </a>
      </li>
    </ul>
    </div>
  </div>
  <% if User.find_by(id: session[:user_id]) != nil %>
  <a class="btn btn-info"href="<%= user_path(User.find_by(id: session[:user_id]).id)%>"><%= User.find_by(id: session[:user_id]).first_name %></a> | 
  <a class="btn btn-danger" href="<%= session_path(session.id) %>" data-confirm="Êtes-vous sûr ?" data-method="DELETE">Se déconnecter</a>
  <% else %>
  <a class="btn btn-primary" href="<%= new_session_path %>">Se connecter</a> | 
  <a class="btn btn-primary" href="<%= new_user_path %>">S'inscrire</a>
  <% end %>

</nav>